# Portia keeps a stable 1.0 assembly identity while packages advance on main.
mode: ContinuousDeployment
next-version: 1.0.0

branches:
  main:
    regex: ^(?<BranchName>master|main)$
    increment: Patch
    is-mainline: true
  develop:
    regex: ^(?<BranchName>dev(elop)?(ment)?)$
    label: "alpha"
  feature:
    regex: ^features?[/-](?<BranchName>.+)
    label: "{BranchName}"
  pull-request:
    regex: ^(pull-requests|pull|pr)[/-](?<BranchName>.+)
    label: "{BranchName}"
  unknown:
    regex: (?<BranchName>.+)
    label: "{BranchName}"
